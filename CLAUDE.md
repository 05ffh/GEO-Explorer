# GEO Explorer

## Retrospectives

每次开发任务前:
1. 读取 `docs/retrospectives/INDEX.md`
2. 搜索与当前任务关键词相关的复盘记录
3. 在实现说明中列出已参考的教训
4. 若本次踩坑超过 30 分钟，按 TEMPLATE.md 格式新增 retrospective 并更新 INDEX.md

## CodeGraph

此项目已初始化 CodeGraph 索引（`.codegraph/codegraph.db`）。使用 `codegraph_*` 工具进行结构性代码探索。
